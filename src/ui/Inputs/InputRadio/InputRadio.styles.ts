import styled from "styled-components";
import { Input } from "../SharedInput.styles";

export const InputRadio = styled(Input)<{ isSquared?: boolean }>`
  margin: 0px 10px 0px 0px;
  accent-color: var(--primary-color);
`;
